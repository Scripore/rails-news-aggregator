
</br>
    <div id="threadtext" class="alert alert-warning">
        <a href="#" class="close" data-dismiss="alert">&times;</a>
       News aggregator for artificial intelligence, machine learning and more.
    </div>
    </br>


    <div class="collapse navbar-collapse">
    <div id="content2">
      <%= form_for('search_phrase') do |f| %>
        <div class="field">
          <%= f.text_field :title, :placeholder => 'Enter in a search phrase (e.g., programming). ', :size => 60%></br>
          <%= @error if @error %>
          </div>

    <% end %>
  </div>
</div>





<ul class="list-group">
    <div id="threadtext"
      <div class="list-group">
        <% @links.each do |link| %>
          <li class="list-group-item">
          <%= link_to(link.title.titleize, link.url) %></br>
          score: <%= link.score%> comments: <%= link_to(link.num_comments, link.permalink)%> time: <%=time_ago_in_words(Time.at(link.created))%> ago 
                 
          </br>

          
          </li>
        <% end %>
      </div>
    </div>
</ul>

<%= javascript_include_tag "//www.google.com/jsapi", "chartkick" %>
<%= line_chart User.group_by_day(:created_at).count %>
